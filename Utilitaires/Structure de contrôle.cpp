// Boucle
while(true){                // Répéter indéfinement : effectue indéfiniment ce qui est dans la boucle  
  Programe
}

while(condition){           // Répéter tant que : effectue indéfiniment ce qui est dans la boucle tant que la condition est vrai
  Programme
}

do{                         // Faire puis Répéter tant que : effectue une fois la boucle puis effectue indéfiniment ce qui est dans la boucle tant que la condition est vrai
  Programme
} while(condition);

for(valeur;condition;incrémentation de la valeur){ // 
  Programme
}

break; // Sors d'une boucle

// Conditions
if(Condition){          // Si : Effectue le programme si la condition est remplie.
  Programme
}
else if(Condition){     // Sinon si : Effectue le programme si la condition si n'est paas remplie et que sa condition est remplie.
  Programme
}
else{                   // Sinon : Effectue le programme si la condition du if et des else if ne sont pas remplies.
  Programme
}


Variable1 = Condition ? True : False; // Affecte une valeur si la condition est remplie, affecte une autre valeur si la condition n'est pas remplie

// Exemple
int var1 = 15;
int var2 = 10;
string Variable1 = var1 > var2 ? "Supérieur" : "Inférieur"; // Variable1 = "Supérieur"

switch var:
case cas 