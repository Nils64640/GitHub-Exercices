int main(){   // Fonction lancé au départ par le programme
  Programme
  return(0)
}

typevariable nom(typevariable parametre){// Fonction se nommant "nom" pouvant être changé par n'importe quel nom avec les mêmes restictions qu'une variable. Peut avoir des variables (parametre) [pas obligatoire dépend si besoin dans le programme]
  programme
  return(resultat)
}

// Exemple
int au_care(int var1){    // Fonction qui met le nombre au carré "n²"
  int result = var1 * var1;
  return(result);
}

float addition(float var1, int var2){    // Fonction qui additione un float et un int
  float result = var1 + var2;
  return(result);
}

void affichage(){
  std::cout << "J'affiche quelque chose";
}